<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to '𝐌𝐔𝐑𝐌𝐔𝐑𝐀', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to '𝚄𝚜𝚎𝚛', users_path, class: 'nav-link' %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to '𝙼𝚢 𝙸𝚗𝚋𝚘𝚡', chats_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to '𝙼𝚎𝚜𝚜𝚊𝚐𝚎𝚜', messages_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item d-flex align-items-center">
            <span class="me-2"><%= current_user.first_name %></span>
            <%= button_to 'Log out', destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger ms-2', form: { class: 'd-inline' } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log in', new_user_session_path, class: 'btn btn-outline-primary ms-2' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>